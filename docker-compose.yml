services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./backend/model_cache:/app/model_cache
    env_file:
      - ./backend/.env
    restart: unless-stopped